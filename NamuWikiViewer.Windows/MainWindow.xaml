<?xml version="1.0" encoding="utf-8" ?>
<winuiex:WindowEx
    x:Class="NamuWikiViewer.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:NamuWikiViewer.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    Title="NamuWikiViewer.Windows"
    MinWidth="800"
    MinHeight="400"
    Closed="OnWindowClosed"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="AppTitleBar"
            Title="나무위키 뷰어"
            Grid.Row="0"
            BackRequested="OnAppTitleBarBackRequested"
            IsPaneToggleButtonVisible="True"
            PaneToggleRequested="OnAppTitleBarPaneToggleRequested">
            <!--  Force Large Size  -->
            <TitleBar.Content>
                <ContentControl />
            </TitleBar.Content>

            <TitleBar.IconSource>
                <ImageIconSource ImageSource="ms-appx:///Assets/logo.png" />
            </TitleBar.IconSource>
        </TitleBar>

        <Frame x:Name="MainFrame" Grid.Row="1" />
    </Grid>
</winuiex:WindowEx>

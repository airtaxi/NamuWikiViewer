<?xml version="1.0" encoding="utf-8" ?>
<winuiex:WindowEx
    x:Class="NamuWikiViewer.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dev="using:DevWinUI"
    xmlns:local="using:NamuWikiViewer.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    Title="NamuWikiViewer.Windows"
    MinWidth="800"
    MinHeight="400"
    Closed="OnWindowClosed"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="AppTitleBar"
            Title="나무위키 뷰어"
            Grid.Row="0"
            BackRequested="OnAppTitleBarBackRequested"
            IsPaneToggleButtonVisible="True"
            PaneToggleRequested="OnAppTitleBarPaneToggleRequested">
            <!--  Force Large Size  -->
            <TitleBar.LeftHeader>
                <Button
                    x:Name="HomeButton"
                    Margin="0,0,12,0"
                    Padding="14"
                    Background="Transparent"
                    BorderThickness="0"
                    Click="OnAppTitleBarHomeClicked"
                    Foreground="{StaticResource ReverseThemeColor}"
                    Visibility="Collapsed">
                    <FontIcon FontSize="14" Glyph="&#xE80F;" />
                </Button>
            </TitleBar.LeftHeader>
            <TitleBar.Content>
                <StackPanel
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="8">
                    <AutoSuggestBox
                        x:Name="TitleBarAutoSuggestBox"
                        Width="300"
                        Height="32"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        PlaceholderText="검색"
                        PreviewKeyDown="OnTitleBarAutoSuggestBoxPreviewKeyDown"
                        QueryIcon="Find"
                        QuerySubmitted="OnTitleBarAutoSuggestBoxQuerySubmitted"
                        TextChanged="OnTitleBarAutoSuggestBoxTextChanged"
                        Visibility="Collapsed" />
                </StackPanel>
            </TitleBar.Content>

            <TitleBar.IconSource>
                <ImageIconSource ImageSource="ms-appx:///Assets/logo.png" />
            </TitleBar.IconSource>
        </TitleBar>

        <Frame x:Name="MainFrame" Grid.Row="1" />

        <Grid
            x:Name="LoadingGrid"
            Grid.RowSpan="2"
            Visibility="Collapsed">
            <Rectangle Fill="{ThemeResource ThemeColor}" Opacity="0.3" />

            <dev:BlurEffectControl
                BlurAmount="10"
                IsBlurEnabled="True"
                IsTintEnabled="False" />

            <StackPanel Spacing="8">
                <ProgressRing
                    Width="100"
                    Height="100"
                    Foreground="{ThemeResource SystemAccentColor}"
                    IsIndeterminate="True" />
                <TextBlock
                    x:Name="LoadingTextBlock"
                    HorizontalAlignment="Center"
                    FontSize="16"
                    FontWeight="Bold" />
            </StackPanel>
        </Grid>
    </Grid>
</winuiex:WindowEx>
